import 'package:flutter/material.dart';
import 'package:get/get.dart';

import '../index.dart';

class SignUpTabletView extends GetView<SignUpController> {
  const SignUpTabletView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("sign up tablet view"),
    );
  }
}
